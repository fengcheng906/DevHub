# 测试方法与实测结果（TESTING）

> 本文件只记录**实际运行过**的测试。没跑过的不写"通过"。

## 测试环境

- Windows + Python 3.13.15
- 运行日期：2026-09-17

## 数据处理助手（datatool/）

**v0.2.0 实测结果（2026-09-18，真实环境运行）：核心 54 项通过，0 失败；GUI 冒烟 15 项通过，0 失败**

| 测试组 | 内容 | 结果 |
|---|---|---|
| 筛选（新增） | 等于/包含/大于/为空/不为空、数字比较防呆（非数字比较值抛错）、非法条件抛错、不改原件 | 7/7 通过 |
| 排序（新增） | 数字升/降序、文字自然顺序（第2组<第10组）、不改原件 | 4/4 通过 |
| 图表数据（新增） | 文字列次数统计、数字列自动分段且总数对账、空列不崩溃 | 3/3 通过 |
| 组合链路（新增） | 清洗→筛选→排序连续执行结果正确 | 1/1 通过 |
| GUI 冒烟（新增） | 列下拉框同步、筛选执行、降序、清除复原、图表窗口可创建、未选文件拦截 | 7/7 通过 |

**研发中发现并修复的真 bug（诚实记录）**：数字降序排序时空白行会跑到最前面 → 已修复为"无论升降序，空白和非数字行永远垫底"，并有测试守住。

exe 打包验证（v0.2.0）：打包成功，实际启动进程存活 8 秒，✅ 通过。成品 `C:\DevHub\05_Output\数据处理助手 v0.2.0.exe`。

### 历史版本实测记录

**核心逻辑测试（test_core.py）实测结果（2026-09-17，v0.1.0，真实环境运行）：36 项通过，0 项失败**

**核心逻辑测试（test_core.py）实测结果（2026-09-17，v0.1.0，真实环境运行）：36 项通过，0 项失败**

| 测试组 | 内容 | 结果 |
|---|---|---|
| 读取 | utf-8-sig 识别、GBK 自动识别且中文不乱码、行短补长截、空文件报错、缺文件报错 | 9/9 通过 |
| 清洗 | 去空格、删空行、删空列（含"表头空白但有内容则保留"）、去重、删含空白行、填充、智能数字列（千分位规范化、混合列不强行转）、默认流程、原始数据不被改动 | 15/15 通过 |
| 统计 | 有效值/缺失/不重复计数、数字列识别、最小/最大/平均、文字列不当数字、文字报告 | 9/9 通过 |
| 导出 | 默认路径命名、读回一致、utf-8-sig 编码、**原始文件字节级不被修改** | 3/3 通过 |

**GUI 冒烟测试（gui_smoke.py，不弹窗用代码模拟）实测结果：8 项全部通过**

窗口创建、原始数据载入、状态栏显示编码、清洗后空行被删、清洗标记、表格数据行可见、导出文件生成且可读回、统计可运行。

**exe 打包验证（2026-09-17，v0.1.0）**

| 项目 | 结果 |
|---|---|
| pyinstaller 打包成功，产出单个 exe（约 12MB） | ✅ 通过 |
| 实际启动 exe，进程正常存活 8 秒（观察后手动关闭） | ✅ 通过（真实环境运行验证） |
| 成品存放位置 | `C:\DevHub\05_Output\数据处理助手 v0.1.0.exe` |

注意：exe 启动验证了"能打开"，exe 内的功能未逐项实测（逻辑与 app.py 同源，已由 36 项核心测试 + 8 项 GUI 冒烟覆盖）。

**研发过程中发现的问题（诚实记录）**：初版统计测试数据里有一行完全重复，导致"不重复个数"和"平均值"的预期值算错（产品逻辑没错，是测试预期写错了），已修正测试数据与预期。

---

## 一、核心逻辑自动化测试（test_core.py）

> 重要环境记录：本项目的测试**必须在真实环境运行**（不加沙盒）。曾发现沙盒内 `rmdir` 对非空目录的行为与真实 Windows 不同（会错误放行），沙盒里的通过/失败结果对文件系统操作不可信。

**运行方式**：
```
cd C:\DevHub\01_Projects\AI-Studio-OS\workbench
python test_core.py
```

**实测结果（2026-09-17，v0.1.5，真实环境运行）：76 项通过，0 项失败**

| 测试组 | 内容 | 结果 |
|---|---|---|
| 测试1 安全检查 | C:\Windows / Program Files / 盘根 / 个人文档目录均被拒绝；普通文件夹被允许 | 5/5 通过 |
| 测试2 扫描与规划 | 生成计划后文件夹零变化；冲突识别、无规则跳过、隐藏文件跳过全部正确 | 7/7 通过 |
| 测试3 执行整理 | 5 个该移的文件正确移入分类文件夹；冲突文件原地不动 | 8/8 通过 |
| 测试4 日志 | 撤销日志（JSON）和人读报告（TXT）均正确生成 | 3/3 通过 |
| 测试5 撤销+清目录 | 5 文件恢复；5 个空分类目录被清理；原有非空目录保留 | 11/11 通过 |
| 测试6 定位最新日志 | 能正确找到最新一份撤销日志 | 1/1 通过 |
| 测试7 边界：目录非空不误删 | 用户放入新文件的目录及其内容完整保留，空目录正常清理 | 4/4 通过 |
| 测试8 按日期归档 | 不按类型过滤全部归档、按月份归类、撤销全恢复、空目录清理 | 7/7 通过 |
| 测试9 自定义规则 | 文本解析往返、5 种错误格式拦截、保存/加载往返、损坏回退默认、自定义规则真实生效 | 11/11 通过 |
| 测试10 递归扫描 | 非递归不深入、递归发现嵌套文件、已归位跳过、记录目录跳过、嵌套执行与撤销回原路径 | 8/8 通过 |
| 测试11 批量重命名 | 序号模式规划与执行、改名撤销恢复、查找替换、名字没变跳过、重名冲突不覆盖、文件消失跳过 | 11/11 通过 |

**研发过程中发现并修复的真实缺陷**：日志文件名时间戳只精确到秒，同一秒内两次整理的撤销日志会互相覆盖——测试中发现后已改为毫秒精度。

**测试环境说明（诚实记录）**：本套件曾在沙盒 shell 中运行，该环境对 rmdir 非空检查的行为与真实 Windows 不一致（会错误放行）。**当前结果均为沙盒外真实环境运行所得**，rmdir 拒绝非空目录的行为已单独验证（WinError 145）。

完整输出见 `workbench/tests/last_test_output.txt`。

## 二、GUI 冒烟测试（2026-09-17 实测通过）

不点鼠标、用代码模拟的方式验证界面行为：

| 验证项 | 结果 |
|---|---|
| 窗口可创建（tkinter 初始化） | ✅ 通过 |
| 扫描预览：8 个文件正确加载进表格 | ✅ 通过 |
| 有文件可移时「执行整理」按钮激活 | ✅ 通过 |
| 空文件夹时「执行整理」按钮禁用 | ✅ 通过 |
| 只有无规则文件时按钮禁用 | ✅ 通过 |

## 三、人工验收（2026-09-17，用户本人实测）

| 验收项 | 结果 |
|---|---|
| 双击 app.py 打开软件（选 Python → 始终） | ✅ 通过 |
| 选择文件夹（浏览到 02_Learning/试一试） | ✅ 通过 |
| 扫描预览：6 行，5 绿色将移动 + 1 灰色跳过 | ✅ 通过 |
| 执行整理：确认弹窗 → 5 文件归入分类文件夹 | ✅ 通过（资源管理器亲眼确认） |
| 撤销上次：成功恢复 5 个文件回原位 | ✅ 通过 |

**验收结论：v0.1 人工验收通过。**
发现的小瑕疵：撤销后空的分类文件夹会留在原地（已列入 v0.2 待办：撤销时自动清理空目录）。

## 五、尚未测试 / 无法自动测试的部分（如实说明）

| 项目 | 状态 |
|---|---|
| 鼠标点击的完整人机交互（选择文件夹的浏览对话框、确认弹窗） | ⚠ 需要真人试用验证 |
| 在真实用户文件夹（如下载目录副本）上的表现 | ⚠ 建议先在测试文件夹里试一次 |
| 双击 app.py 直接启动 | ⚠ 与 Python 安装时的文件关联有关，需在用户桌面实测 |

## 六、.exe 打包验证（2026-09-17，v0.1.5）

| 项目 | 结果 |
|---|---|
| pyinstaller 打包成功，产出单个 exe（约 10MB） | ✅ 通过 |
| 实际启动 exe，进程正常存活、窗口可创建（8 秒观察后关闭） | ✅ 通过（真实环境运行验证） |
| 成品存放位置 | `C:\DevHub\05_Output\智能文件工作台 v0.1.5.exe` |

注意：exe 启动验证了"能打开"，**exe 内的整理/改名功能未逐项实测**（逻辑与 app.py 同源，已由 76 项核心测试覆盖）。

## 六之补、拖拽功能冒烟（2026-09-17，v0.1.6）

| 场景 | 结果 |
|---|---|
| 拖入普通文件夹 → 正确选中 | ✅ 通过 |
| 拖入单个文件 → 自动用其所在文件夹 | ✅ 通过 |
| 拖入带空格路径（花括号格式）→ 正确解析 | ✅ 通过 |
| 拖入 C:\Windows → 安全拦截 | ✅ 通过 |
| 修复记录：无花括号路径走 splitlist 会被反斜杠转义破坏，已改为按花括号判断 | ✅ 修复并回归 |

真实鼠标拖拽的手感（拖哪个区域有效）需真人试用确认。

## 七、建议的人工验收步骤（5 分钟）

1. 在 C:\DevHub\02_Learning 里新建一个 `试一试` 文件夹，随便复制几个不同类型的文件进去
2. 双击 `C:\DevHub\05_Output\智能文件工作台 v0.1.5.exe` 打开软件
3. 选这个文件夹 → 扫描预览 → 检查计划 → 执行 → 去资源管理器看结果
4. 回到软件点「撤销上次」→ 确认文件全部回来了
